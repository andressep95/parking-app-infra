# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
tfplan
plan_output.txt
outputs.json

# Drift reports (generados en CI, no commitear)
drift-reports/

# Secrets y vars locales
*.auto.tfvars
terraform.tfvars
!terraform.tfvars.example

# OS
.DS_Store
